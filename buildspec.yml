version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install for static HTML site"
  build:
    commands:
      - echo "Nothing to build, just deploying static files"

artifacts:
  files:
    - '**/*'
  base-directory: ./   # Root of repo, where index.html is
